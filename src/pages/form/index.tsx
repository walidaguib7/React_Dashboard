import { Box } from "@mui/material";
import Header from "../../components/Header";

const Form = () => {
  return (
    <Box marginLeft={2}>
      <Header title="Form" subtitle="Form content" />
    </Box>
  );
};

export default Form;
