import { Box } from "@mui/material";
import Header from "../../components/Header";

const Dashboard = () => {
  return (
    <Box marginLeft={2}>
      <Header title="Dashboard" subtitle="Dashboard content" />
    </Box>
  );
};

export default Dashboard;
