import { Box } from "@mui/material";
import Header from "../../components/Header";

const Pie = () => {
  return (
    <Box marginLeft={2}>
      <Header title="Pie" subtitle="Pie content" />
    </Box>
  );
};

export default Pie;
