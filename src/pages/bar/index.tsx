import { Box } from "@mui/material";
import Header from "../../components/Header";

const Bar = () => {
  return (
    <Box marginLeft={2}>
      <Header title="Bar" subtitle="Bar" />
    </Box>
  );
};

export default Bar;
