import { Box } from "@mui/material";
import Header from "../../components/Header";

const Line = () => {
  return (
    <Box marginLeft={2}>
      <Header title="Line" subtitle="Line content" />
    </Box>
  );
};

export default Line;
