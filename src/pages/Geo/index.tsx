import { Box } from "@mui/material";
import Header from "../../components/Header";

const Geo = () => {
  return (
    <Box marginLeft={2}>
      <Header title="Geo" subtitle="Geo content" />
    </Box>
  );
};

export default Geo;
