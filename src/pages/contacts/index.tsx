import { Box } from "@mui/material";
import Header from "../../components/Header";

const Contacts = () => {
  return (
    <Box marginLeft={2}>
      <Header title="Contacts" subtitle="Contacts content" />
    </Box>
  );
};

export default Contacts;
